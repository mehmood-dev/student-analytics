# Frontend

Minimal React frontend powered by Vite.

## Run locally

From the repository root:

```bash
cd frontend
npm install
npm run dev
```

The development server will print its local URL. The frontend is intentionally small so the application UI can be built on top of it later.
